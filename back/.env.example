NODE_ENV=development
PORT=4000

# Conexión local por partes (desarrollo)
PGHOST=localhost
PGPORT=5432
PGDATABASE=enfermerosya
PGUSER=postgres
PGPASSWORD=

# En producción (Vercel + Neon) se usa la cadena completa. Ej:
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Secreto para firmar los JWT (obligatorio en producción)
JWT_SECRET=

